export enum SQL {
    WHERE = 'WHERE',
    OR = 'OR',
    AND = 'AND',
    INNER= 'INNER',
    LEFT= 'LEFT',
    RIGHT= 'RIGHT',
    FULL= 'FULL',
    OUTER= 'OUTER',
    SELECT = 'SELECT',
    JOIN = 'JOIN',
    FROM = 'FROM'
}